<%= render "layouts/navbar" %>

<div class="card-index">
  <div class="card-deck">
    <% @users.each do |user| %>
      <%= link_to user_path(user) do %>
        <div class="card">
          <% if user.image.blank? %>
            <%= image_tag 'no_image.png', class: "matching__person-default-img" %>
          <% else %>
            <%= image_tag user.image, class: "matching__person-default-img" %>
          <% end %>
          <div class="card-body text-center">
            <%= user.name %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>